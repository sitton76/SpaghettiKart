#include <libultraship/libultraship.h>
#include <libultra/gbi.h>

#include "Handles.h"

namespace Editor {
    Handles::Handles() {
        Pos = &pos;
        Rot = &rot;
    }

    void Handles::Load() {

    }

    void Handles::Tick() {

    }

    void Handles::Draw() {

    }
}